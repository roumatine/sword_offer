#include "..\..\head.h"

class Solution
{
public:
    double findMedianSortedArrays(vector<int> &nums1, vector<int> &nums2)
    {
        if (a.size() > b.size())
        {
            swap(a, b);
        }
        int m = a.size();
        int n = b.size();
    }
};