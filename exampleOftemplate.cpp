#include "head.h"

template <typename, type1, typename, type2>

type1 Max(type1 a, type2 b) {
    return a > b ? a : b;
}

void main() {
    cout << "Max = " << max(1, 2) << endl;
}
// operattor ">" to type change